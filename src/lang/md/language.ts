import { isLowerLetter } from "../../text_manip";

// replace -- on copy also, dunno how yet tho
function revise(content: readonly string): string {
    let foundPeriod = false;
    let out = content.slice();

    for (const ch of ) {
        if (isLowerLetter(ch)) {
            
        }
    }
}